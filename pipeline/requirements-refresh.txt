# Slim requirements for the daily CRE price refresh workflow.
# Only includes packages actually imported by the refresh path
# (refresh.py --source cre_prices --no-qa). Pin to match
# requirements.txt for the shared subset to avoid version drift
# between local and CI.
# Full pipeline dev install: pip install -r requirements.txt

httpx==0.26.0
tenacity==8.2.3
click==8.1.7
structlog==24.1.0
rich==13.7.0
pandas==2.2.3
pyarrow==19.0.0          # required by pandas read_parquet/to_parquet (storage/raw_zone.py)
sqlalchemy==2.0.27
pydantic==2.5.0          # models/station.py, models/match.py
geohash2==1.1
rapidfuzz==3.6.1
Unidecode==1.3.8
python-dateutil==2.9.0
pytz==2024.1
