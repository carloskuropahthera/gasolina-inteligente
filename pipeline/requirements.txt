# ═══════════════════════════════════════════════════════════
# Gasolina Inteligente — Pipeline Requirements
# Python 3.11+
# Install: pip install -r requirements.txt
# ═══════════════════════════════════════════════════════════

# ── Core data manipulation ──────────────────────────────────
pandas==2.2.0
pyarrow==15.0.0          # Parquet I/O, required by pandas for .parquet read/write
pydantic==2.5.0          # Data models + field validation
sqlalchemy==2.0.27       # ORM + raw SQL (sqlite3 is built-in, no extra driver needed)

# ── HTTP / Scraping ─────────────────────────────────────────
httpx==0.26.0            # Modern async-capable HTTP client
tenacity==8.2.3          # Retry logic with exponential backoff
beautifulsoup4==4.12.3   # HTML parsing for brand locator pages
lxml==5.1.0              # Faster HTML/XML parser backend for BeautifulSoup
playwright==1.41.0       # Headless Chromium for JS-rendered locator pages

# ── Geospatial ──────────────────────────────────────────────
shapely==2.0.3           # Geometry operations (polygon containment for QA-05)
pyproj==3.6.1            # Coordinate reference system transforms
geohash2==1.1            # Geohash encoding for blocking keys
haversine==2.8.0         # Haversine distance calculation (meters)

# ── Text / Fuzzy Matching ────────────────────────────────────
rapidfuzz==3.6.1         # Fast fuzzy string matching (C-extension: Levenshtein, token sort)
Unidecode==1.3.8         # Accent/diacritic stripping for normalization
jellyfish==1.0.3         # Phonetic matching: Soundex, NYSIIS, Metaphone

# ── Analytics ───────────────────────────────────────────────
duckdb==0.10.0           # In-process OLAP queries on Parquet + SQLite

# ── Observability / CLI ─────────────────────────────────────
structlog==24.1.0        # Structured logging with context binding
rich==13.7.0             # CLI tables, progress bars, pretty panels

# ── Orchestration (production; optional for MVP) ─────────────
# prefect==2.16.0        # Uncomment when moving to Prefect Cloud
# Comment out above line for MVP to keep install lean

# ── Testing ─────────────────────────────────────────────────
pytest==8.0.0
pytest-httpx==0.28.0     # Mock httpx HTTP calls in unit tests
faker==22.0.0            # Synthetic test data generation

# ── Data validation (optional but recommended) ───────────────
pandera==0.18.3          # DataFrame schema validation (for DENUE CSV format change detection)

# ── Utilities ───────────────────────────────────────────────
python-dateutil==2.9.0   # Robust date parsing
pytz==2024.1             # Timezone handling (Mexico/General, America/Mexico_City)
click==8.1.7             # CLI argument parsing for scripts
tqdm==4.66.1             # Progress bars (fallback if rich not available)
